[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npx serve dist --listen tcp://0.0.0.0:${PORT:-8080}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
